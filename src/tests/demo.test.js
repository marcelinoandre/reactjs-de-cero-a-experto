describe('Test Demo', () => {
  it('should  to be equals the messages', () => {
    const message1 = 'Bora lá vamos que vamos';
    const message2 = 'Bora lá vamos que vamos';

    expect(message1).toBe(message2);
  });
});
